// Shared id for the admin create/edit form <dialog>. Trigger buttons reference
// it through the native Invoker Commands API (command / commandfor) so the
// browser owns the dialog open/close lifecycle.
export const ADMIN_FORM_DIALOG_ID = 'admin-form-dialog';
